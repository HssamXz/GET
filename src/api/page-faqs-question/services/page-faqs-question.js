'use strict';

/**
 * page-faqs-question service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::page-faqs-question.page-faqs-question');
