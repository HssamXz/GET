'use strict';

/**
 * page-faqs-question router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::page-faqs-question.page-faqs-question');
