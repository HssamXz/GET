'use strict';

/**
 * tourismeinformation service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::tourismeinformation.tourismeinformation');
